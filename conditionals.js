function addFigures(fig1, fig2, fig3 ){
let addFig= fig1 + fig2 + fig3
console.log(addFig);
 }

addFigures(20, 30, 40);


function addThreeNumbers(num1, num2, num3){
	let total=num1 + num2 + num3 + ' students in total'
	return total
}

let inTotal = addThreeNumbers(30, 20, 10)
console.log(inTotal)
